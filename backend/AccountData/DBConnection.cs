namespace SmartStudyAI.Backend.Data;

public class DBConnection
{
    // Sprint 1 placeholder for now
    // SQL connection logic will be added later when the database is connected

    public string ConnectionString { get; set; } =
        "Server=localhost;Database=SmartStudyAI;Trusted_Connection=True;TrustServerCertificate=True;";
}